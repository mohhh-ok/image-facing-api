# pre-commit フック: コミット前に gitleaks で秘密情報を検出する。
# セットアップ:
#   pip install pre-commit   (または brew install pre-commit)
#   pre-commit install
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
