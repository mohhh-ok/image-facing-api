# データ・秘密情報
data/
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# モデル（大きい）。Railway CLI デプロイ（railway up）でアップロードするため、
# あえて gitignore しない（除外すると railway up が拾わず COPY models/ が失敗する）。
# ただし git には add/commit しないこと（80MB）。GitHub 連携に切り替える場合は Git LFS 管理に。
# models/*.onnx

# OS / エディタ
.DS_Store
.idea/
.vscode/
