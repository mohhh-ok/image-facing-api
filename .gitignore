# データ・秘密情報
data/
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# モデル（大きい）。git では無視（誤コミット防止）。
# Railway へは `railway up --no-gitignore` でアップロードする（.railwayignore が .venv/data/.env 等を除外）。
# .gitignore のみ尊重する素の `railway up` だと onnx が載らず model_loaded:false になるので注意。
models/*.onnx

# OS / エディタ
.DS_Store
.idea/
.vscode/
